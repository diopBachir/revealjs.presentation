% load packages
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{eso-pic}
\usepackage{fancyhdr}
\usepackage{sectsty}
\usepackage{float}
% \usepackage{fontspec}
\usepackage{titlesec}
% \usepackage[T1]{fontspec}
\usepackage[french,english]{babel}
\usepackage{polyglossia}
\setmainfont{Times New Roman}

%% Set page size with a wider right margin
\geometry{a4paper, total={170mm,257mm}, left=25mm, top=10mm, bottom=20mm, right=10mm}

%% Let's define some colours
\definecolor{light}{HTML}{E6E6FA}
\definecolor{teal}{HTML}{003660}
\definecolor{highlight}{HTML}{800080}
\definecolor{dark}{HTML}{330033}

%% Let's add the border on the right hand side 
\AddToShipoutPicture{% 
    \AtPageLowerLeft{% 
        \put(\LenToUnit{\dimexpr\paperwidth-21cm},0cm){% 
            \color{light}\rule{1.5cm}{\LenToUnit\paperheight}%
          }%
     }%
    % logo ugb
    \AtPageLowerLeft{% start the bar at the bottom right of the page
        \put(\LenToUnit{\dimexpr\paperwidth-21.02cm},01cm){% move it to the top right
            \color{light}\includegraphics[width=1.5cm]{images/logo_ugb.png}
          }%
     }%
    % logo ird
    \AtPageLowerLeft{% start the bar at the bottom right of the page
    \put(\LenToUnit{\dimexpr\paperwidth-20.98cm},03cm){% move it to the top right
        \color{light}\includegraphics[width=1.4cm]{images/ird.png}
      }%
    }%
    % logo leidi
    \AtPageLowerLeft{% start the bar at the bottom right of the page
    \put(\LenToUnit{\dimexpr\paperwidth-20.88cm},06cm){% move it to the top right
        \color{light}\includegraphics[width=1.3cm]{images/leidi.png}
      }%
    }%
    % logo hsm
    \AtPageLowerLeft{% start the bar at the bottom right of the page
    \put(\LenToUnit{\dimexpr\paperwidth-20.98cm},09cm){% move it to the top right
        \color{light}\includegraphics[width=1.5cm]{images/logo-hsm.png}
      }%
    }%
     % logo hsm
     \AtPageLowerLeft{% start the bar at the bottom right of the page
     \put(\LenToUnit{\dimexpr\paperwidth-21.06cm},012cm){% move it to the top right
         \color{light}\includegraphics[width=1.5cm]{images/cecc.png}
       }%
     }%
}

%% Style the page number
\fancypagestyle{mystyle}{
  \fancyhf{}
  \renewcommand\headrulewidth{0pt}
  \fancyfoot[R]{\textcolor{gray}{\fontsize{16}{12.8}\selectfont p. \thepage}}
  \renewcommand\footrulewidth{1pt}
  \fancyfootoffset{0cm}
}
\setlength{\footskip}{20pt}

%% style the chapter/section fonts
\chapterfont{\color{teal}\fontsize{20}{16.8}\selectfont}
\sectionfont{\color{teal}\fontsize{20}{16.8}\selectfont}
\subsectionfont{\color{teal}\fontsize{14}{16.8}\selectfont}
\titleformat{\subsection}
  {\color{teal}\sffamily\Large\bfseries}{\thesection}{1em}{}[{\color{teal}\titlerule[2pt]}]
  
% left align title
\makeatletter
\renewcommand{\maketitle}{\bgroup\setlength{\parindent}{0pt}
\begin{flushleft}
  {\huge\textbf{\MakeUppercase{\@title}}} \vspace{0.3cm} \newline
  {\Large {\@subtitle}} \newline
  \@author
\end{flushleft}\egroup
}
\makeatother

%% Use some custom fonts
% \setsansfont{Ubuntu}[
%     Path=_extensions/nrennie/PrettyPDF/Ubuntu/,
%     Scale=0.9,
%     Extension = .ttf,
%     UprightFont=*-Regular,
%     BoldFont=*-Bold,
%     ItalicFont=*-Italic,
%     ]

% \setmainfont{Ubuntu}[
%     Path=_extensions/nrennie/PrettyPDF/Ubuntu/,
%     Scale=0.9,
%     Extension = .ttf,
%     UprightFont=*-Regular,
%     BoldFont=*-Bold,
%     ItalicFont=*-Italic,
%     ]
